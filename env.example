# =============================================================================
# BlogFlow Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Supabase via Vercel Integration)
# -----------------------------------------------------------------------------
# Primary database URL for Drizzle ORM (REQUIRED)
# Use the Transaction pooling URL from Supabase for serverless compatibility
# This should be a pooled connection on port 6543, as recommended for serverless compatibility
# Example: postgresql://user:pass@host.supabase.co:6543/postgres?sslmode=require
DRIZZLE_DATABASE_URL=

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# Authentication (Better Auth)
# -----------------------------------------------------------------------------
# Secret key for JWT signing (REQUIRED)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Base URL for your application server (OPTIONAL in Vercel)
# - Vercel: Automatically uses VERCEL_URL (no need to set this!)
# - Local dev: http://localhost:3000
# - Custom: https://yourdomain.com (only if not using Vercel)
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Google OAuth (Optional - for "Sign in with Google")
# -----------------------------------------------------------------------------
# Google OAuth Client ID and Secret
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (development)
#    or https://yourdomain.com/api/auth/callback/google (production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Client-side flag to enable/disable Google sign-in button
# Set to "true" when Google OAuth is configured
NEXT_PUBLIC_GOOGLE_OAUTH_ENABLED=

# -----------------------------------------------------------------------------
# Webflow Integration
# -----------------------------------------------------------------------------
# Workspace API token for deploying components (REQUIRED for component deployment)
# Get from: https://webflow.com/dashboard/account/apps
WEBFLOW_WORKSPACE_API_TOKEN=

# Webflow API token (Site specific scope) for CMS operations (ex. for blog platform idea if syncing content to CMS for the specific site)
# Get from: Site Settings > Integrations > API Access
WEBFLOW_API_TOKEN=

# Webflow CMS Collection IDs (get after creating collections in Phase 3)
WEBFLOW_POSTS_COLLECTION_ID=
WEBFLOW_PEOPLE_COLLECTION_ID=

# -----------------------------------------------------------------------------
# CORS Configuration (Optional - hardcoded in lib/auth/config.ts and api routes)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for CORS
# Default origins: https://blogflow-three.webflow.io, http://localhost:3000
# Only modify if you need to add additional domains (e.g., custom Webflow domain)
# ALLOWED_CORS_ORIGINS=https://blogflow-three.webflow.io,http://localhost:3000,https://custom-domain.com

# -----------------------------------------------------------------------------
# Public Environment Variables (Available in Browser)
# -----------------------------------------------------------------------------
# API base URL for client-side requests (OPTIONAL in Vercel)
# - Vercel: Automatically uses VERCEL_URL (no need to set this!)
# - Local dev: Uses window.location.origin automatically
# - Custom: https://yourdomain.com (only if not using Vercel)
# Note: Usually you don't need to set this at all!
NEXT_PUBLIC_API_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Cloud Provider Integrations
# -----------------------------------------------------------------------------

# Encryption secret for securing OAuth tokens and sensitive data (REQUIRED)
# Generate with: openssl rand -hex 32
# This must be exactly 64 hex characters (32 bytes)
ENCRYPTION_SECRET=

# Vercel OAuth Integration (for database provisioning and deployments)
# Create an OAuth app at: https://vercel.com/account/tokens
# Then visit: https://vercel.com/dashboard/integrations/new
# Redirect URI: http://localhost:3000/api/integrations/vercel/callback (development)
#               https://yourdomain.com/api/integrations/vercel/callback (production)
# Note: Prefixed with BLOGFLOW_ to avoid conflicts with Vercel's reserved env vars
BLOGFLOW_VERCEL_CLIENT_ID=
BLOGFLOW_VERCEL_CLIENT_SECRET=

# Vercel OAuth URL Configuration (choose ONE option)
# Option 1: Provide the full OAuth authorize URL (recommended for integrations with custom slugs)
# Example: https://vercel.com/integrations/webcn/oauth/authorize
BLOGFLOW_VERCEL_OAUTH_URL=

# Option 2: Provide just the integration slug (if using a Vercel integration)
# The URL will be constructed as: https://vercel.com/integrations/{slug}/oauth/authorize
# Example: webcn
# Leave blank for generic OAuth URL (deprecated)
BLOGFLOW_VERCEL_INTEGRATION_SLUG=
