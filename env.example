# =============================================================================
# BlogFlow Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Supabase via Vercel Integration)
# -----------------------------------------------------------------------------
# Primary database URL for Drizzle ORM (connection pooling)
POSTGRES_URL=

# Non-pooling connection (for migrations)
POSTGRES_URL_NON_POOLING=

# Individual connection parameters (auto-populated by Vercel)
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Prisma-specific URL (if using Prisma in future)
POSTGRES_PRISMA_URL=

# -----------------------------------------------------------------------------
# Supabase Configuration (Optional - for future features)
# -----------------------------------------------------------------------------
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# Authentication (Better Auth)
# -----------------------------------------------------------------------------
# Secret key for JWT signing (REQUIRED)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Base URL for your application (REQUIRED)
# Development: http://localhost:3000
# Production: https://yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Google OAuth (Optional - for "Sign in with Google")
# -----------------------------------------------------------------------------
# Google OAuth Client ID and Secret
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (development)
#    or https://yourdomain.com/api/auth/callback/google (production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Client-side flag to enable/disable Google sign-in button
# Set to "true" when Google OAuth is configured
NEXT_PUBLIC_GOOGLE_OAUTH_ENABLED=

# -----------------------------------------------------------------------------
# Webflow Integration
# -----------------------------------------------------------------------------
# Workspace API token for deploying components (REQUIRED for component deployment)
# Get from: https://webflow.com/dashboard/account/apps
WEBFLOW_WORKSPACE_API_TOKEN=

# Webflow API token for CMS operations (REQUIRED for blog platform)
# Get from: Site Settings > Integrations > API Access
WEBFLOW_API_TOKEN=

# Webflow CMS Collection IDs (get after creating collections in Phase 3)
WEBFLOW_POSTS_COLLECTION_ID=
WEBFLOW_PEOPLE_COLLECTION_ID=

# -----------------------------------------------------------------------------
# Public Environment Variables (Available in Browser)
# -----------------------------------------------------------------------------
# API base URL for client-side requests (REQUIRED)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# =============================================================================
# COPY-PASTE TEMPLATE (for .env file)
# =============================================================================
# Copy everything below this line to your .env file and fill in the values

POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
POSTGRES_PRISMA_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXT_PUBLIC_GOOGLE_OAUTH_ENABLED=
WEBFLOW_WORKSPACE_API_TOKEN=
WEBFLOW_API_TOKEN=
WEBFLOW_POSTS_COLLECTION_ID=
WEBFLOW_PEOPLE_COLLECTION_ID=
NEXT_PUBLIC_API_URL=http://localhost:3000